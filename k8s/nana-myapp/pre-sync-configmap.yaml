apiVersion: v1
kind: ConfigMap
metadata:
  name: pre-sync-config
  namespace: myapp
data:
  ARGOCD_APP_NAME: "myapp-argo-application"
  ARGOCD_REPO_URL: "https://github.com/YanivMendiuk/W.H.Y.git"
  WEBHOOK_HOST: "webhook.webhook.svc.cluster.local"
  WEBHOOK_PORT: "8181"
